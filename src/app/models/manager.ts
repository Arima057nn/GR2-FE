export interface ListManager {
  id: string;
  name: string;
  content: number;
  duedate: string;
  category: number;
  status: number;
}

export interface ProcessingManager {
  id: string;
  name: string;
  content: number;
  duedate: string;
  category: number;
  status: number;
}

export interface AcceptManager {
  id: string;
  name: string;
  content: number;
  duedate: string;
  category: number;
  status: number;
}
